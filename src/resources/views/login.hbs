<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đăng nhập Bluemoon</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #0d6efd;
            --bg-color: #f8f9fa;
            --text-color: #343a40;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html,
        body {
            height: 100%;
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
        }

        .container {
            display: flex;
            height: 100vh;
            width: 100%;
        }

        .left {
            flex: 1;
            background-image: url('https://cms.thainguyen.vn/documents/1329041/2264722/1.CUC+BAC+1.jpg/5745b2af-9970-4c75-b5d9-5010da28111a?t=1722235658467');
            background-size: cover;
            background-position: center;
            animation: zoomIn 8s ease-in-out infinite alternate;
        }

        @keyframes zoomIn {
            from {
                background-size: 100%;
            }

            to {
                background-size: 110%;
            }
        }

        .right {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px;
        }

        .login-box {
            width: 100%;
            max-width: 400px;
            background: #ffffff;
            border-radius: 20px;
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
            padding: 40px;
            transition: all 0.3s ease;
        }

        .login-box:hover {
            transform: translateY(-6px);
            box-shadow: 0 16px 45px rgba(0, 0, 0, 0.15);
        }

        .login-box h2 {
            font-size: 28px;
            margin-bottom: 28px;
            color: var(--text-color);
            text-align: center;
        }

        .login-box h2 span {
            color: var(--primary-color);
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 8px;
            color: #495057;
        }

        .form-group input {
            width: 100%;
            padding: 14px;
            font-size: 14px;
            border: 1px solid #ced4da;
            border-radius: 10px;
            transition: 0.3s;
        }

        .form-group input:focus {
            border-color: var(--primary-color);
            outline: none;
            box-shadow: 0 0 0 4px rgba(13, 110, 253, 0.2);
        }

        .options {
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 14px;
            margin-bottom: 24px;
        }

        .options label {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .options a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
        }

        .options a:hover {
            text-decoration: underline;
        }

        .login-btn {
            width: 100%;
            padding: 14px;
            font-size: 16px;
            font-weight: 600;
            color: #fff;
            background-color: var(--primary-color);
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: 0.3s;
        }

        .login-btn:hover {
            background-color: #0b5ed7;
        }

        .error-msg {
            color: #dc3545;
            font-size: 14px;
            margin-bottom: 16px;
            text-align: center;
        }

        @media (max-width: 992px) {
            .container {
                flex-direction: column;
            }

            .left {
                height: 300px;
            }

            .right {
                padding: 20px;
            }

            .login-box {
                padding: 30px;
                max-width: 350px;
            }
        }

        @media (max-width: 576px) {
            .login-box {
                padding: 24px;
            }

            .login-box h2 {
                font-size: 24px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="left"></div>

        <div class="right">
            <div class="login-box">
                <h2>Chào mừng đến chung cư <span>Bluemoon</span>!</h2>

                {{#if error}}
                <p class="error-msg">{{error}}</p>
                {{/if}}

                <form method="POST" action="/login">
                    <div class="form-group">
                        <label for="email">Tên đăng nhập</label>
                        <input type="text" id="email" name="email" placeholder="Nhập tên đăng nhập" required
                            value="{{email}}">
                    </div>

                    <div class="form-group">
                        <label for="password">Mật khẩu</label>
                        <input type="password" id="password" name="password" placeholder="Nhập mật khẩu" required>
                    </div>

                    <div class="options">
                        <label><input type="checkbox" name="remember"> Lưu đăng nhập</label>
                        <a href="#">Quên mật khẩu?</a>
                    </div>

                    <button type="submit" class="login-btn">Đăng nhập</button>
                </form>
            </div>
        </div>
    </div>
</body>

</html>