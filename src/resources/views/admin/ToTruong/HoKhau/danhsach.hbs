<div class="container mt-5">
  <h2 class="mb-4">📋 Danh sách hộ khẩu</h2>

  <form class="mb-4">
    <div class="row g-3">
      <div class="col-md-4">
        <label for="filterHoTen" class="form-label">Họ tên chủ hộ</label>
        <input type="text" class="form-control" id="filterHoTen" placeholder="Nhập họ tên">
      </div>
      <div class="col-md-4">
        <label for="filterCanHo" class="form-label">ID Căn hộ</label>
        <input type="text" class="form-control" id="filterCanHo" placeholder="Nhập số căn hộ">
      </div>
      <div class="col-md-4">
        <label for="filterSoHoKhau" class="form-label">Số sổ hộ khẩu</label>
        <input type="text" class="form-control" id="filterSoHoKhau" placeholder="Nhập số sổ hộ khẩu">
      </div>
    </div>

    <div class="mt-3 d-flex gap-2">
      <button type="button" class="btn btn-primary" onclick="filterTable()">🔍 Lọc</button>
      <button type="button" class="btn btn-secondary" onclick="resetFilter()">Làm mới</button>
      <a href="/admin/hokhau/add" class="btn btn-success ms-auto">+ Thêm mới</a>
    </div>
  </form>

  <div class="table-responsive">
    <table class="table table-bordered table-hover align-middle text-center">
      <thead class="table-dark">
        <tr>
          <th>ID Sổ hộ khẩu</th>
          <th>ID Căn hộ</th>
          <th>Số Sổ hộ khẩu</th>
          <th>Họ tên chủ hộ</th>
          <th>Thông tin thêm</th>
          <th>Xem chi tiết</th>
        </tr>
      </thead>
      <tbody>
        {{#each hokhaus}}
          <tr>
            <td>{{this.idSoHoKhau}}</td>
            <td>{{this.idCanHo}}</td>
            <td>{{this.soSoHoKhau}}</td>
            <td>{{this.hoTenChu}}</td>
            <td>{{this.thongTinThem}}</td>
            <td>
              <a href="/admin/hokhau/{{this.idSoHoKhau}}" class="btn btn-info btn-sm">ℹ️</a>
            </td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>

<script>
  function filterTable() {
    const hoTenFilter = document.getElementById('filterHoTen').value.toLowerCase();
    const canHoFilter = document.getElementById('filterCanHo').value.toLowerCase();
    const soHoKhauFilter = document.getElementById('filterSoHoKhau').value.toLowerCase();

    const rows = document.querySelectorAll('tbody tr');

    rows.forEach(row => {
      const idSoHoKhau = row.cells[0].textContent.toLowerCase();
      const idCanHo = row.cells[1].textContent.toLowerCase();
      const soSoHoKhau = row.cells[2].textContent.toLowerCase();
      const hoTenChu = row.cells[3].textContent.toLowerCase();

      const matchHoTen = hoTenChu.includes(hoTenFilter);
      const matchCanHo = idCanHo.includes(canHoFilter);
      const matchSoHoKhau = soSoHoKhau.includes(soHoKhauFilter);

      row.style.display = (matchHoTen && matchCanHo && matchSoHoKhau) ? '' : 'none';
    });
  }

  function resetFilter() {
    document.getElementById('filterHoTen').value = '';
    document.getElementById('filterCanHo').value = '';
    document.getElementById('filterSoHoKhau').value = '';
    filterTable();
  }
</script>
